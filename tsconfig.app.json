{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": [
      "chart.js" // Đảm bảo 'chart.js' được liệt kê ở đây để TypeScript tìm thấy khai báo kiểu
    ]
  },
  "files": [
    "main.ts"
  ],
  "include": [
    "**/*.d.ts",
    "**/*.ts" // Đảm bảo các tệp .ts của bạn được bao gồm
  ]
}
